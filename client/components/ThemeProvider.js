import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#124116'
    },
    secondary: {
      main: '#ffc400'
    } //#a5d6a7
  },
});

export default theme;
